const FileUpload = () => {
  return (
    <div>
      <form action="">
        <input type="file" className="file-input" />
        <button className="btn">Upload</button>
      </form>
    </div>
  );
};

export default FileUpload;
